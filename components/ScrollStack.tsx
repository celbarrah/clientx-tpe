/**
 * ScrollStack — ReactBits component (TypeScript wrapper)
 * Source: https://www.reactbits.dev/components/scroll-stack
 *
 * Re-exports the JSX version with proper TypeScript types.
 * Modify ScrollStack.jsx (auto-generated) for animation logic.
 * Modify ScrollStack.css for base card styles.
 */
"use client";

// eslint-disable-next-line @typescript-eslint/no-require-imports
const { default: ScrollStack, ScrollStackItem } = require("./ScrollStack.jsx");

export { ScrollStackItem };
export default ScrollStack;
